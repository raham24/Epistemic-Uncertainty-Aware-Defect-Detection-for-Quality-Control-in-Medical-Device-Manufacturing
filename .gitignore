# Generated dataset (large; reproducible from generator.py given the seed)
data/

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Editor
*.swp

figs/

# Local-only: tests / scratch and superseded older scripts -- not for GitHub.
# Only the current prof-facing code (generator*, mlp*) is tracked in root.
misc/
legacy/

# Cluster sweep outputs + generated matrix files (large / regenerable). The
# cluster/ scripts + cluster_analysis.ipynb ARE tracked; their outputs are not.
results/cluster/
cluster/logs/
cluster/datasets.tsv
cluster/runs.tsv
cluster/manifest.json

docs/